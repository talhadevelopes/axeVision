{
  "version": 2,
  "builds": [
    {
      "src": "apps/platform/**",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "buildCommand": "pnpm build --filter @axeVision/platform"
}